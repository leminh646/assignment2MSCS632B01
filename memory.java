public class Main {
    public static void main(String[] args) {
        Integer[] numbers = {1, 2, 3, 4, 5};  // dynamically allocated array
        System.out.println(numbers[0]);

        // Java's garbage collector automatically frees memory when objects are no longer in use
    }
}
